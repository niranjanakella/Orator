# Core Dependencies
numpy>=1.24.0
soundfile>=0.13.1

# ONNX Runtime (for TTS inference - no PyTorch dependency)
onnxruntime>=1.18.0

# Audio & System Integration
pygame>=2.6.1
pynput>=1.8.1
pyperclip>=1.11.0

# macOS Menu Bar
rumps>=0.4.0

# Utilities
loguru>=0.7.3
pyobjc>=10.0

# CLI Framework
click>=8.0.0

# System Requirement (not pip installable):
# espeak-ng - Install via: brew install espeak-ng (macOS) or apt install espeak-ng (Linux)
